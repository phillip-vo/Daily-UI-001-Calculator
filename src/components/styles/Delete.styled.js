import styled from "styled-components";

export const Delete = styled.img`
  width: 25px;
  height: 25px;
  margin: 25px;
  cursor: pointer;

  &:hover {
    opacity: 0.9;
    transform: scale(0.98);
  }
`;
